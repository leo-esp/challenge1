import styled from "styled-components";
import { Form as FormRaw } from "formik";

export const PokeForm = styled.div``;

export const Form = styled(FormRaw)`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
`;
