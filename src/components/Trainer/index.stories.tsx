import { Meta, StoryObj } from "@storybook/react";

import Trainer, { Props as TrainerProps } from "./index";

export default {
  title: "Atoms/Trainer",
  component: Trainer,
} as Meta<TrainerProps>;

export const Default: StoryObj<TrainerProps> = {};
